import React, { Component } from 'react';

class AccountInfo extends Component {
  render() {
    return (
      <section>
        Account Info
      </section>
    );
  }
}

export default AccountInfo;
