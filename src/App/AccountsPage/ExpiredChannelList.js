import React, { Component } from 'react';

class ExpiredChannelList extends Component {
  render() {
    return (
      <section>
        Expired Channel Details
      </section>
    );
  }
}

export default ExpiredChannelList;
