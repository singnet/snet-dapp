export const configuration = {
  debug: {
    /**
     * For the convenience of debugging services inside the sandbox, we make the purchase of a skippable
     */
    skipPurchase: true,
    /**
     * Skip getting service details
     */
    useMockServiceDetails: true,
  },
};
