export const useStyles = theme => ({
  PurchaseDescription: {
    marginTop: 33,
    color: theme.palette.text.alertBoxColor,
    fontSize: 14,
    letterSpacing: 0.25,
    lineHeight: "21px",
  },
});
